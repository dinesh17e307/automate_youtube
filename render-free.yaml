# Render FREE tier — $0/month (single service, no Redis, no Docker)
# Deploy: Render Dashboard → New → Blueprint → connect repo
# Use this file OR rename to render.yaml

services:
  - type: web
    name: kids-youtube
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run build:free
    startCommand: NODE_ENV=production npm run start:free
    healthCheckPath: /api/health
    envVars:
      - key: FREE_TIER
        value: "true"
      - key: SERVE_FRONTEND
        value: "true"
      - key: INLINE_WORKER
        value: "true"
      - key: STORAGE_PATH
        value: /opt/render/project/src/storage
      - key: DATABASE_URL
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: CRON_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        sync: false
      - key: FRONTEND_URL
        sync: false
      - key: YOUTUBE_CLIENT_ID
        sync: false
      - key: YOUTUBE_CLIENT_SECRET
        sync: false
      # Optional — auto-detected from RENDER_EXTERNAL_URL if omitted
      - key: YOUTUBE_REDIRECT_URI
        sync: false
